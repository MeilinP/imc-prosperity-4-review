# CLAUDE.md — 这个项目的操作准则

## 这是什么

**一场考试。用户拿到了自己的卷子,现在在复盘。**

五轮 = 五道大题。每道题只做四件事:

1. 我当时写了什么
2. 我当时考了多少、错在哪
3. **重新审题** —— 这道题真正在考什么
4. **重新写出正确答案** —— 并证明这确实是正确的

**这个 repo 存在的唯一目的:证明用户知道正确答案是什么,并且能写出来。**

不是"我的分析"。不是"一种可行的做法"。是**正确**。

---

## 「正确」的定义(每次动手前重读)

| | 不合格 | 合格 |
|---|---|---|
| 正确的方法 | 我自己想了一套 | **这个领域公认的标准做法**,叫得出名字 |
| 正确的考点 | 我猜 IMC 想考什么 | **有证据**:顶尖队伍都在做这件事 / 数据本身逼出这个结论 |
| 正确的答案 | 我推导出来的规则 | **能被独立验证**:分数、对照、或与顶尖队伍一致 |

**判据:如果一个懂行的人来看,他会说"对,这就是标准答案",而不是"这是你的一种做法"。**

---

## 正确答案从哪来 —— 先查,再推

**不要每次从零推导。手上有现成的答案来源,先用它们:**

| 来源 | 位置 | 有什么 |
|---|---|---|
| **Leo 队(算法榜 96)** | `~/Desktop/prosperity review/reference/IMC-Prosperity-4-Review/` | 五轮策略代码、**策略规格 md**、研究 notebook(期权、均值回归、ETF 回归、bot 分析)、README 的分轮纪实 |
| 其他公开 repo | 需要时上网找 | 同上 |
| 用户之前做过的分析 | `~/Desktop/prosperity review/_复盘工作区/` | 7 个 notebook、31 张图、旧的复盘 md。**大部分是垃圾,但里面有做过的工作,挖出来比重做便宜** |
| 官方数据 + 用户日志 | `~/Desktop/prosperity review/round1..5/` | 三天赛前数据 + 计分日日志 |

**顺序:先看顶尖队伍怎么做的 → 用数据验证他们对不对 → 写成答案。**
**不要先自己推一套,再回头对照。那样得到的是我的答案,不是正确答案。**

---

## 每轮的四个部分

| | 内容 | 验收 | 难度 |
|---|---|---|---|
| 1 · 我当时写了什么 | 公允价公式、符号定义、初始化/更新/边界规则、下单条件 | 照着能把算法重写一遍 | **白给,别费力** |
| 2 · 我当时的表现 | 官方分数、成交与持仓、逐 tick 回放 | 重放解释日志里 **100%** 的成交 | **白给,别费力** |
| 3 · 重新审题 | 这商品是什么、IMC 在考什么 | 每条结论有可证伪判据 + 与顶尖队伍的做法对得上 | **重点** |
| 4 · 正确答案 | 状态/控制/目标函数、值函数、报价规则、逐条对照表、**分数** | **分数 ≥ 算法榜第 96 名** 且对照表无空行 | **重点** |

**1 和 2 是机械活,快速做完。全部精力放 3 和 4。**

---

## 交付形态

每轮两个文件:

```
rounds/0X/README.md        一个 md（不是四个），嵌图，是结论
research/roundX.ipynb      一个 notebook，是过程：加载→画图→检验→结论→推规则
```

**notebook 是同一套 SOP 跑五遍,只换数据。** 流程固定:

```
1  加载数据，画出来
2  趋势 / 均值回归 / 随机漫步？        方差比 + 回归斜率
3  偏离能撑多久？                      自相关半衰期
4  短周期能不能预测？                  订单簿不平衡
5  和别的商品有没有固定关系？          恒等式 / 协整（ADF）
6  能赚多少？                          流量 × 价差
7  谁的成交有信息？                    对手方分析
8  由以上推交易规则
9  样本外验证
```

五个 notebook 长得一样,本身就是证据:结论是流程跑出来的,不是一轮一轮编的。

---

## 沟通规则(反复违反过,记住)

1. **第一句是大白话结论,不带数字。** 数字往后放。
2. **不汇报技术细节。** 用户当场验证不了的东西,说了等于没说。
3. **不汇报我自己修 bug。** 修了就修了,合并进结论里。
4. **不确认就不动文件。** 提完方案就停,不要写"确认了我就开始做"。
5. **消息要短。** 用户读不完的东西等于没写。
6. **一次问一个问题。** 问一堆,我就会顺手把答案也替他写了。

---

## 已经确立、不要重新讨论的事

- **回测的口径**:回测重放的是一段固定录像,它无法对新算法做出反应。所以只能说"同一段录像上 A 比 B 好",不能说"上线能得多少分"。这条已经和用户对齐,写进 README 了。
- **回测器状态**:R1 误差 0.13%、R2 0.02%(可信);R3 FRUIT 差 1,733;R4 差 9.8%(不可信);R5 未验。用 `prosperity4bt`,`--limit` 每个产品一个 flag。
- **不提 AI 辅助**:用户已决定 README 不写这件事。
- **语言**:repo 全英文(用户当年的提交原件除外)。

---

## 当前状态

| 轮 | 交付 | 第 3 步 | 第 4 步 |
|---|---|---|---|
| R1 | README + answer.md + notebook(10 图) | ✅ 含订单簿不平衡 | ✅ 15 条规则,14 条有证据 |
| R2 | README + answer.md + notebook(5 图) | ✅ | ✅ 10 条规则全有证据;闸门实现未验,已标注 |
| R3 | README + answer.md + notebook(7 图) | ✅ | ✅ 11 条规则全有证据;ATM 波动率错价未测,已列为 Open |
| R4 | README + answer.md + notebook(4 图) | ✅ | ✅ 结论=**不要用对手方信号**(留一法推翻旧结论) |
| R5 | README + answer.md + notebook(8 图) | ✅ | ✅ 11 条规则全有证据;38.9 万那个数已证伪,列为 Recorded not adopted |

**已证的关键结论(勿重推)**
- 挂单量:流量中位 5、最大 13,挂 10 手接到 99.8% ⇒ 挂多大无所谓,Leo 的"挂满"不必抄
- ASH 目标持仓 ∝ 偏离,系数 −0.5,与半衰期自洽;饱和点 K **不可测**,是偏好
- **深度不平衡(三档)**预测外层公允价,R1 系数 +2.4/+2.6 撑到 h=50;**必须用外层公允价当目标**,
  用 best 中点会因内层小单同时改变价和量而产生假象(曾把两个口径的符号弄反)
- 不平衡**不能穿价**:预测 4.9,穿价成本 8 ⇒ 只能偏移报价
- R2 计分日**两个信号同时失效**(回归 + 不平衡);R1 计分日两者都完好 ⇒ 不是重建假象
- **R3 流量是单边的**:虚值 voucher 只有人卖给我(138~334 手/天),没人买。取双边较小值会全算成 0 —— 
  期权做市靠标的对冲,不需要在期权本身来回。十个 voucher 合计仅 2,104/天 < PACK 一个的 4,573
- **价差=1 时不能挂内部**:`best_bid+1` 就是卖价,edge 从 +0.50 翻成 −0.50。价差 ≤1 只能挂在触及价
- **订单簿不平衡只在 R1/R2 市场有效**(系数 2.4~2.6,t≈40);R3/R4 市场完全没有(|t|≤1.3) ⇒ 不可跨市场搬
- **R5 扫全 50 产品**找到 4 个持续负自相关(另 3 个是 57 名队没提的);但**都穿不过价差**,
  只能当报价偏移。他们报的 DISHES 38.9 万**证伪**:限仓 10 手 × 全天振幅 1302 = 上限约 13,020
- **R4 对手方信号不可用**:Mark 14 在 FRUIT 四天同号、在 PACK 赛前三天同号却在计分日翻负,
  而赛前数据分不出这两者。**合并三天算 t 值会凭空造出信号** —— 必须逐日报 + 留出计分日

**最大的缺口:第 4 部分还没有分数。**
